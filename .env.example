# Database settings (optional, defaults are for local dev)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=medical_billing

# Server port (optional)
PORT=5000

# Option 1 (recommended): Provide full Firebase service account JSON as a single-line string.
# Copy the JSON contents of the service account file and paste here (escape newlines or keep it as a single line).
# Example (not real):
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"..."}

# Option 2: Provide the path to the service account file and set GOOGLE_APPLICATION_CREDENTIALS env var
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-file.json
